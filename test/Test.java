package test;

import Gui.Accueil;


/**
 *La classe Test est la classe principale qui lance l'application.
 *Elle cree une instance de la classe Accueil et la rend visible à l'utilisateur.
 */
public class Test {

	/**
	 * La methode main est la méthode principale de la classe Test.
	 * Elle cree une instance de la classe Accueil et la rend visible à l'utilisateur.
	 * @param args les arguments de la ligne de commande (non utilisés dans cette application)
	 */
	public static void main(String[] args) {
		new Accueil().setVisible(true);
	}

 
}
               
               